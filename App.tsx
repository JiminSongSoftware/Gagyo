/**
 * App entry point.
 *
 * When using expo-router with the app/ directory, the routing is handled
 * by the files in the app/ directory. app/_layout.tsx is the root layout
 * that wraps all routes with providers (i18n, theme, etc.).
 *
 * This file can be used for any app-level initialization that needs to happen
 * before the router is mounted.
 */

export default function App() {
  return null; // expo-router app/_layout.tsx handles everything
}
